{
  "name": "stengg-mini-project",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && prisma generate && node index.js",
    "build": "cd client && npm i && npm run build",
    "install:client": "cd client && npm i",
    "start:client": "cd client && npm run start"
  },
  "author": "Tu Nguyen",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "fast-csv": "^4.3.6",
    "mongodb": "^6.1.0",
    "multer": "^1.4.5-lts.1",
    "prisma": "^5.3.1"
  }
}
